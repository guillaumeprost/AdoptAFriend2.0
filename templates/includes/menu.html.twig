<nav class="navbar navbar-expand-lg sticky-top">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('homepage') }}">
            <div class="brand-icon me-2">
                <i class="fas fa-heart"></i>
            </div>
            <span class="brand-text">AdoptAFriend</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link nav-link-modern" href="{{ path('homepage') }}">
                        <i class="fas fa-home me-1"></i>
                        Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-modern" href="{{ path('animal_search') }}">
                        <i class="fas fa-paw me-1"></i>
                        Animaux
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-modern organisation-link" href="{{ path('organisation_search') }}">
                        <i class="fas fa-building me-1"></i>
                        Associations
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link nav-link-modern" href="{{ path('user_dashboard') }}">
                            <i class="fas fa-user me-1"></i>
                            Mon profil
                        </a>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link user-menu dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="user-avatar me-2">
                                <i class="fas fa-user"></i>
                            </div>
                            {{ app.user.firstName }}
                        </a>
                        <ul class="dropdown-menu modern-dropdown">
                            <li><a class="dropdown-item modern-dropdown-item" href="{{ path('user_dashboard') }}">
                                <i class="fas fa-tachometer-alt me-2"></i>Tableau de bord
                            </a></li>
                            <li><a class="dropdown-item modern-dropdown-item" href="{{ path('user_update') }}">
                                <i class="fas fa-user-edit me-2"></i>Modifier mon profil
                            </a></li>
                            <li><a class="dropdown-item modern-dropdown-item" href="{{ path('user_animals') }}">
                                <i class="fas fa-paw me-2"></i>Mes animaux
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item modern-dropdown-item" href="{{ path('logout') }}">
                                <i class="fas fa-sign-out-alt me-2"></i>Se déconnecter
                            </a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-primary" href="{{ path('register') }}">
                            <i class="fas fa-user-plus me-1"></i>
                            S'inscrire
                        </a>
                    </li>
                    <li class="nav-item ms-2">
                        <a class="btn btn-outline-primary" href="{{ path('login') }}">
                            <i class="fas fa-sign-in-alt me-1"></i>
                            Se connecter
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>