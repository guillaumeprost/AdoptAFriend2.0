<nav class="navbar navbar-expand-lg sticky-top">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('homepage') }}">
            <div class="brand-icon me-2">
                <i class="fas fa-heart"></i>
            </div>
            <span class="brand-text">AdoptAFriend</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            {% set currentRoute = app.request.attributes.get('_route') %}
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link nav-link-modern {{ currentRoute == 'homepage' ? 'nav-active' : '' }}" href="{{ path('homepage') }}"{% if currentRoute == 'homepage' %} aria-current="page"{% endif %}>
                        <i class="fas fa-home me-1"></i>
                        Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-modern {{ currentRoute == 'animal_search' ? 'nav-active' : '' }}" href="{{ path('animal_search') }}"{% if currentRoute == 'animal_search' %} aria-current="page"{% endif %}>
                        <i class="fas fa-paw me-1"></i>
                        Animaux
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-modern organisation-link {{ currentRoute == 'organisation_search' ? 'nav-active' : '' }}" href="{{ path('organisation_search') }}"{% if currentRoute == 'organisation_search' %} aria-current="page"{% endif %}>
                        <i class="fas fa-building me-1"></i>
                        Associations
                    </a>
                </li>
            </ul>

            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link user-menu dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <div class="user-avatar me-2">
                                <i class="fas fa-user"></i>
                            </div>
                            {{ app.user.firstName }}
                        </a>
                        <ul class="dropdown-menu modern-dropdown">
                            <li><a class="dropdown-item modern-dropdown-item {{ currentRoute == 'user_dashboard' ? 'active' : '' }}" href="{{ path('user_dashboard') }}">
                                <i class="fas fa-tachometer-alt me-2"></i>Tableau de bord
                            </a></li>
                            <li><a class="dropdown-item modern-dropdown-item {{ currentRoute == 'user_update' ? 'active' : '' }}" href="{{ path('user_update') }}">
                                <i class="fas fa-user-edit me-2"></i>Modifier mon profil
                            </a></li>
                            <li><a class="dropdown-item modern-dropdown-item {{ currentRoute == 'user_animals' ? 'active' : '' }}" href="{{ path('user_animals') }}">
                                <i class="fas fa-paw me-2"></i>Mes animaux
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item modern-dropdown-item" href="{{ path('logout') }}">
                                <i class="fas fa-sign-out-alt me-2"></i>Se déconnecter
                            </a></li>
                        </ul>
                    </li>
                {% else %}
                    <div class="d-flex flex-column flex-lg-row align-items-stretch">
                        <li class="nav-item mb-2 mb-lg-0">
                            <a class="btn btn-primary w-100" href="{{ path('register') }}">
                                <i class="fas fa-user-plus me-1"></i>
                                S'inscrire
                            </a>
                        </li>
                        <li class="nav-item ms-lg-2">
                            <a class="btn btn-outline-primary w-100" href="{{ path('login') }}">
                                <i class="fas fa-sign-in-alt me-1"></i>
                                Se connecter
                            </a>
                        </li>
                    </div>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>