<nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('homepage') }}">
            🐶 Adopt a Friend 🐱
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
                aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <span class="nav-link disabled px-2">|</span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('animal_search') }}">À adopter</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('organisation_search') }}">Les associations</a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('user_animals') }}">Mes animaux</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('animal_create', {'type':'dog'}) }}">Ajouter un animal</a>
                    </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav mb-2 mb-lg-0">
                {% if app.user %}
                    {% if app.user.organisation %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('organisation_dashboard') }}">{{ app.user.organisation.name }}</a>
                        </li>
                        <li class="nav-item">
                            <span class="nav-link disabled px-2">|</span>
                        </li>
                    {% endif %}

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarProfile" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            {{ app.user.firstname ?? app.user.email }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarProfile">
                            <li><a class="dropdown-item" href="{{ path('user_dashboard') }}">🏠 Dashboard</a></li>
                            <li><a class="dropdown-item" href="{{ path('user_update') }}">👤 Mon profil</a></li>
                            <li><a class="dropdown-item" href="{{ path('adoption-request_list') }}">📋 Demandes d'adoption</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('login') }}">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>