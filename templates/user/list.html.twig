{% extends 'base.html.twig' %}

{% block content %}

    <div class="container">
        <h1 class="mt-5">Utilisateurs</h1>

        <ul>
        {% for user in users %}
            <li>
                <p>{{ user.firstName }} {{ user.name }} ({{ user.email }})</p>
            </li>
        {% endfor %}
        </ul>
    </div>

{% endblock %}